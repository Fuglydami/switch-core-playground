/**
 * Re-exports the React Native token values from @switch/tokens/rn.
 * These are unitless numbers — do NOT add 'px'. React Native's StyleSheet
 * expects bare numbers for all dimension values.
 */
export { default as tokens } from '@switch/tokens/rn';
