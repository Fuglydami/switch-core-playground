{
  "buildCommand": "pnpm run build --filter=@switch/react && pnpm run build --filter=playground",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/playground/.next"
}
